-- Optional demo seed (run after registering a real user, or use setup script)
-- Prefer creating accounts via the registration flow.
SELECT 'Use registration UI to create the first account.' AS notice;
